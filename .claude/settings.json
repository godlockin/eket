{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {"pattern": "**/*.rs", "command": "cd rust && cargo fmt --check"},
          {"pattern": "**/*.py", "command": "ruff check --fix"}
        ]
      }
    ]
  }
}
