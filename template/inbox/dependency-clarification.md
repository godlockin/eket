# 数据依赖追问模板

**用途**: 当检测到项目缺少数据源、API、认证等必要配置时，自动创建此文件追问用户

---

## 🚨 检测到缺失的依赖配置

智能体在分析项目需求时，发现以下必要信息尚未提供。请补充以下信息，以便项目能够正确构建和部署。

---

## 1. 数据源配置 ⚠️ 必选

### 1.1 数据库

您的项目是否需要数据库？

- [ ] **否** - 项目不需要持久化存储
- [ ] **是** - 请填写：
  - 类型：`MySQL` / `PostgreSQL` / `MongoDB` / `SQLite` / 其他：____
  - 部署方式：`本地` / `Docker` / `云服务` / 已有实例
  - 预估数据量：`<1GB` / `1-10GB` / `10-100GB` / `>100GB`

**连接配置** (不要填写真实密码，使用占位符):
```bash
# 示例格式
DATABASE_URL=postgresql://user:PASSWORD@localhost:5432/dbname
# 或
MONGODB_URI=mongodb://localhost:27017/mydb
```

### 1.2 外部 API 依赖

您的项目是否需要调用外部 API？

- [ ] **否** - 不调用任何外部 API
- [ ] **是** - 请列出：

| API 名称 | 用途 | 认证方式 | 端点 |
|---------|------|---------|------|
| 例：Gemini | AI 对话 | API Key | https://generativelanguage.googleapis.com |
| ____ | ____ | ____ | ____ |
| ____ | ____ | ____ | ____ |

### 1.3 文件存储

您的项目是否需要存储文件？

- [ ] **否** - 不需要文件存储
- [ ] **是** - 存储类型：
  - [ ] 本地文件系统
  - [ ] AWS S3
  - [ ] 阿里云 OSS
  - [ ] 其他：____

---

## 2. 认证和密钥管理 ⚠️ 必选

### 2.1 API 密钥

您需要使用哪些 API 密钥？

```bash
# 请列出需要的环境变量名（不要填写真实值）
GEMINI_API_KEY=your_api_key_here
OPENAI_API_KEY=your_api_key_here
# 其他：____
```

### 2.2 用户认证

您的项目是否需要用户认证功能？

- [ ] **否** - 不需要用户认证
- [ ] **是** - 认证方式：
  - [ ] 用户名/密码
  - [ ] 邮箱验证码
  - [ ] OAuth (Google/GitHub/微信/支付宝)
  - [ ] 手机短信验证码
  - [ ] JWT Token
  - [ ] 其他：____

---

## 3. 基础设施配置

### 3.1 部署目标

您的项目将部署到哪里？

- [ ] **本地开发** - 仅本地运行
- [ ] **Docker 容器**
- [ ] **云服务**:
  - [ ] Vercel / Netlify (前端)
  - [ ] AWS / 阿里云 / 腾讯云
  - [ ] Kubernetes 集群
- [ ] **其他**: ____

### 3.2 域名和 HTTPS

- [ ] 已有域名：____
- [ ] 需要 HTTPS：是 / 否
- [ ] CDN 需求：是 / 否

---

## 4. 第三方服务

### 4.1 邮件服务

- [ ] 不需要
- [ ] 需要：`SendGrid` / `AWS SES` / `阿里云邮件推送` / 其他：____

### 4.2 短信服务

- [ ] 不需要
- [ ] 需要：`Twilio` / `阿里云短信` / `腾讯云短信` / 其他：____

### 4.3 支付服务

- [ ] 不需要
- [ ] 需要：`Stripe` / `支付宝` / `微信支付` / 其他：____

### 4.4 监控和日志

- [ ] 不需要
- [ ] 需要：`DataDog` / `New Relic` / `Prometheus+Grafana` / `ELK`

---

## 5. 安全和合规

### 5.1 安全要求

- [ ] 基础安全（HTTPS、CORS、速率限制）
- [ ] 高级安全（WAF、DDoS 防护、安全审计）
- [ ] 合规要求：`GDPR` / `等保` / `HIPAA` / 其他：____

---

## 6. 性能要求

### 6.1 性能指标

- 预期 QPS（每秒查询数）: ____
- 响应时间要求：____ ms
- 预期并发用户数：____

---

## 回复方式

请在下方填写您的回答，然后保存并发送给智能体：

---

## 人类回复

**填写日期**: ____

### 数据源配置

```
数据库：[填写类型]
连接方式：[填写部署方式]
外部 API: [列出 API 名称]
文件存储：[填写类型]
```

### 认证配置

```
API 密钥：[列出环境变量名]
用户认证：[填写认证方式]
```

### 基础设施

```
部署目标：[填写部署方式]
域名/HTTPS: [填写配置]
```

### 其他说明

```
[其他需要说明的内容]
```

---

**状态**: `awaiting_human_response`
**创建时间**: $(date -Iseconds)
