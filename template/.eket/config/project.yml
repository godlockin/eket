# EKET 项目配置模块

version: "2.0.0"

project:
  name: "{{PROJECT_NAME}}"
  root: "{{PROJECT_ROOT}}"
  initialized_at: "{{TIMESTAMP}}"
  stage: "phase_1"  # phase_1, phase_2, phase_3, ...

mode: "claude_code"
