# Retro stub — PR #77 (TASK-052)

- merged_at: 20260418T043620Z
- merge_sha: d95132ee2acf494211094bad43c6858873fe4d2a
- author: godlockin
- title: fix(ci): TASK-052 broadcast peter-evans PR + drop benchmark paths filter
- url: https://github.com/godlockin/eket/pull/77

## TODO (24h SLA)
- [ ] What worked
- [ ] What hurt + root cause
- [ ] Lessons → 沉淀到 confluence/memory/{RULE-RETENTION-LESSONS,BORROWED-WISDOM}.md
- [ ] 移动本文件到 confluence/memory/retrospectives/2026/

## PR body snapshot

```
## Summary
修复 PR #75/#76 暴露的两个 governance carry-over：

1. **broadcast push 被 `enforce_admins` 拦截** → 改用 `peter-evans/create-pull-request@v6` 自动开 PR + auto-merge
2. **benchmark required-check + paths filter 死锁** → 删除 PR 触发器的 `paths:` 过滤

## Changes
- `.github/workflows/post-merge-broadcast.yml`: 改用 peter-evans，labels=`auto-broadcast,bot-pr,infra-only,docs-only` 全豁免 + workflow if 防递归
- `.github/workflows/perf-baseline.yml`: PR 触发器去掉 `paths:`
- `jira/tickets/TASK-052.md`: 新 ticket，dispatched_by: human:steven

## Tests
```
Test Suites: 56 passed, 56 total
Tests:       1153 passed, 1153 total
```

YAML lint: safe_load 通过

## AI-Review
AI-Review: claude opus 4.5 — 复盘 #75/#76 两次 admin merge carry-over，本 PR 闭环。peter-evans 是 GitHub 官方推荐的 bot-creates-PR 标准模式（绕过 enforce_admins）。bot-loop 通过 if 表达式防递归。

## Ref
Ref: TASK-052

## Post-merge
Merge 后手动把 `benchmark` 加回 required contexts (AC-4):
```
gh api -X PATCH /repos/godlockin/eket/branches/main/protection/required_status_checks \
  -f 'contexts[]=verify-test-evidence' -f 'contexts[]=block-self-loop' \
  -f 'contexts[]=dual-engine' -f 'contexts[]=require-debrief' -f 'contexts[]=benchmark'
```

```
