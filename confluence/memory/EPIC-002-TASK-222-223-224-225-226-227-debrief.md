# EPIC-002 Closure Debrief — TASK-222 / 223 / 224 / 225 / 226 / 227

**EPIC**: EPIC-002 — 吸收 addyosmani/agent-skills 方法论，构建 EKET expert/skill 7-section anatomy 体系
**周期**: 2026-04-22 ~ 2026-04-28（含 1 hotfix 与一次 misdiagnosis 澄清）
**Master**: master-001
**Slaver**: slaver-001
**仓库**: 主仓 `eket` (feature/TASK-226a-rules-fixtures) + subrepo `eket-experts-extended` (12 feature 分支)

---

## 1. EPIC-002 概览

EPIC-002 的目标是把 [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills) 的 skill 设计方法论吸收到 EKET 的 expert/skill 体系内：把"角色 + 知识 + 校验"三件套抽象为统一的 7-section anatomy（Identity / Activation / Anti-Rationalization / Capabilities / Workflow / Verification / Self-Improvement），并配套校验脚本、CI、模板、INDEX 与跨仓 codemod，使 EKET 的 7 default 专家与 53 optional 专家能批量产出标准化产物。

**6 个 ticket 节奏**：
- TASK-222：5 default 专家追加 anti-rationalization 表 + 补 fullstack/tester 两位 default。
- TASK-223：7-section anatomy 在 architect/backend/frontend/fullstack/product/tester/ux 落地（PR-A/B/C 三批）。
- TASK-224：`check-skill-anatomy.sh` + fixtures + `anatomy-check.yml` workflow。
- TASK-225：subrepo `eket-experts-extended` 53 optional 专家批量注入 3-section TODO skeleton 的 `codemod-inject-3sections.sh` + 单测；11 个 PR 分支按 ai/data/dev/... 拆分。
- TASK-226：`check-pr-size.sh` + `pr-size-check` workflow + RULES Rule4/5/8/9 + mock fixture 改造。
- TASK-227：scripts/template polish（`--all`、`--exclude`、anatomy template、CI informational job）+ default & optional INDEX.md + 老脚本 7 角色对齐 + hotfix。

---

## 2. 各 Ticket 速览

### TASK-222 — Default 专家 anti-rationalization 补齐
- **交付物**：5 个 default 专家（architect/backend/frontend/product/ux）追加 anti-rationalization 表；新增 fullstack.md + tester.md 两位 default 专家。
- **关键决策**：anti-rationalization 表用 markdown 表格（行=理由化模式 / 列=反制动作），保证后续 grep 校验。
- **遗留**：无。

### TASK-223 — 7-section anatomy 在 7 default 专家落地
- **交付物**：PR-A（architect/backend）+ SKILL-ANATOMY-TEMPLATE.md；PR-B（frontend/fullstack）；PR-C（product/tester/ux）+ tester.md description / rationalizations_count 修复。
- **关键决策**：模板先于专家落地（PR-A 内一并交付），保证后续 PR 引用一致；tester AC-3 hotfix 走主分支直 commit（小幅修复）。
- **遗留 AC**：**AC-5 system:doctor 灰度验证 ⏳**（等部署灰度）。

### TASK-224 — Anatomy 校验脚本 + CI
- **交付物**：`scripts/check-skill-anatomy.sh`（minimal/strict 两档）+ fixtures 双平台 + `.github/workflows/anatomy-check.yml`。
- **关键决策**：minimal 模式仅校验 7 节标题存在 + ≥3 anti-rationalization 行；Verification 节中 bash 块视为信息性 warn，避免阻塞 PR。
- **遗留**：无；hotfix（b5d20e96）补 7 default Verification 节 ≥3 checkbox + frontmatter parser 修复。

### TASK-225 — Subrepo 53 optional 专家 codemod
- **交付物**：`scripts/codemod-inject-3sections.sh` + 单测 + fixtures（PR-00 主仓）；subrepo 11 个 `feature/TASK-225-*` 分支共 11 commits，覆盖 53 文件注入 3 节 TODO skeleton。
- **关键决策**：subrepo 11 分支故意保留未 merge 到 `miao`，**等 EPIC 收尾整体 push**（master-pr-c-review.md:62-66）。
- **遗留 AC**：**AC-2 codemod 实跑回填 ⏳** —— 物理上等 subrepo 11 分支 merge 进 miao 后立刻 `--all` 跑过即关闭。**非 codemod 漏跑**（见 §4 misdiagnosis 澄清）。

### TASK-226 — PR size 校验 + RULES 补强
- **交付物**：`scripts/check-pr-size.sh` + `.github/workflows/pr-size-check.yml`；RULES Rule4/5（PR/commit 行数硬上限）+ Rule8/9（mock fixture 强制）；fixtures 改用 mock-based。
- **关键决策**：PR ≤500 行硬阈值，warn 通道留给 docs/test-only commit；mock fixture 取代真业务样本，避免泄漏。
- **遗留**：无。

### TASK-227 — Scripts/template polish + INDEX 收口
- **交付物**：commit-1 scripts/template/CI 5 文件（`--all`、`--exclude=INDEX.md`、anatomy template、informational job）；commit-2 default + optional INDEX.md（草稿入库 + 主仓镜像 augment）；commit-3 init-project + load-agent-profile 7 角色对齐；commit-4 hotfix `set -u` + 空数组守卫；subrepo `feature/TASK-227-index` INDEX augment（1694b023）。
- **关键决策**：subrepo INDEX 与主仓镜像内容同源；`--all` 在 miao 基线上 default 7/7 PASS、optional 0/53（待 TASK-225 merge 回填）。
- **遗留**：无新增；TASK-225 AC-2 联动状态见 §5。

---

## 3. 三条核心 Lessons Learned（来源：TASK-227 master-review.md）

1. **`set -u` + 空数组陷阱**
   bash `set -u` 下访问 `${arr[@]}` 当 `arr` 未定义/空时会触发 `unbound variable`，让 `--all` 路径上的 trailer-less 脚本直接 exit。
   修复：`${arr[@]+"${arr[@]}"}`，或在函数顶部先 `arr=()` 显式初始化。
   slaver-001 自检发现并独立修复（commit-4 +1/-1），无需 Master 介入——Deviation Rule 1（自检 → 提单 commit 修）的标准动作。

2. **AC 跨 ticket 联动回填的 sequencing 陷阱**
   TASK-225 AC-2 依赖 codemod 跑过的内容 merge 进 trunk 才能验证；EPIC-002 故意延后 subrepo merge → AC-2 在物理上无法在 TASK-227 内闭环。
   预防：拆 ticket 时若 ACx 依赖跨仓 merge，明确标注「AC-x 在 EPIC push 步骤验收」，避免后续 slaver 在中间 ticket 误判为 codemod bug。

3. **草稿文件 git ?? 状态识别**
   slaver §4 分析时仅基于"逻辑 ground truth"（`git ls-files`）判定 INDEX.md 不存在，Master 实测 `ls` + `git status` 才发现已有未跟踪草稿。
   预防：分析阶段若涉及"是否新建"，必须 `git status --short <path>` + `ls -la <path>` 双重确认，不能只看 `git ls-files`。

---

## 4. 跨 Ticket 经验

### Anti-rationalization 红线两次正面成立
- **TASK-225**（master-pr-c-review.md）：slaver 完成 11 PR 分支后，未在 commit body 标 AC-2 ✅，主动上报「subrepo 等 EPIC push」决策权 → 上交 Master。
- **TASK-227**（master-review.md §澄清）：slaver 自报 `--all` optional 0/53 PASS，**保守判断**为"待 EPIC push 后回填"，未在 commit message 假宣告 AC-2 闭环；Master 复核确认是 sequencing 而非 codemod bug。

两次同一 slaver 在不同 ticket 都选择"上报"而非"顺手宣告"——anti-rationalization 不是文档摆设，是真在闭环里起作用。

### Commit message 严禁 trailer
EPIC-002 全程 13+ commit 0 trailer：无 `Co-Authored-By` / `Generated-with` / `Approved-Large-PR-By`。Master 反复强调：trailer 是审计噪声 + 自我闭环风险。本 EPIC 没破例。

### ≤500 行健康区间始终守住
13 主仓 commit + 12 subrepo commit，**全部** ≤500 行（最大 PR-A ≈ +480/-30）。pr-size-check workflow 在 EPIC 中段（TASK-226）才上线，但人工节奏一直贴着该阈值——证明 Rule4/5 是"事后形式化"已存在的工程节奏，不是新约束。

---

## 5. EPIC 收尾顺序

实际状态（以本仓 git log 为准）：
- **Subrepo `eket-experts-extended`**：11 个 TASK-225 分支 + 1 TASK-227-index 分支已通过 `--no-ff` merge 进 `miao` 并 push origin（依据 Master 收尾计划，状态以 subrepo `git log --oneline miao` 为最终凭证）。
- **主仓 `eket`**：`feature/TASK-226a-rules-fixtures` 已 push origin 并开 PR #134（base=testing），本 debrief commit 即触发最后一轮 EKET 自有 CI 重跑。

收尾顺序：
1. ✅ subrepo 11 TASK-225 + 1 TASK-227-index `--no-ff` merge → miao → push（已执行）。
2. ⏳ subrepo merge 完成后在 miao 上跑 `bash scripts/check-skill-anatomy.sh --all` 回填 **TASK-225 AC-2**（在主仓 PR 合并前完成）。
3. ⏳ 主仓 PR #134 通过三 EKET 自有 CI（pr-body-check / pr-reviewer-check / debrief-check）+ 业务 CI 后 merge 进 testing → main → miao（按分支策略）。
4. ⏳ TASK-223 AC-5（system:doctor 灰度验证）单独排期，**不阻塞 EPIC-002 整体 close**。

---

## 6. 收尾断言

- 6/6 ticket 实现 + 文档 + review 已闭环；2 项遗留 AC（TASK-225 AC-2 / TASK-223 AC-5）路径明确。
- 3 条 lesson 已沉淀在本文件 + TASK-227 master-review.md 双索引可查。
- Anti-rationalization 红线两次正面成立 + 0 trailer + ≤500 行三项工程纪律全程未破。

🔓 EPIC-002 实现工作完全收口，待 PR #134 三 CI 绿灯后即可整体 merge。
