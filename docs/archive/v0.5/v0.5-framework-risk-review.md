# EKET 框架 v0.5 风险审查报告

**审查时间**: 2026-03-23
**审查依据**: v0.5 升级内容 + 软件工程原则 + 团队协作最佳实践
**审查范围**: 11 项新策略 + SOP 流程 + 配置机制

---

## 一、Executive Summary

### 1.1 风险等级总览

| 风险等级 | 数量 | 状态 |
|------|------|------|
| 🔴 Critical | 7 | 需要立即修复 |
| 🟡 High | 9 | 需要本周修复 |
| 🟢 Medium | 8 | 需要规划修复 |
| ⚪ Low | 5 | 可选优化 |

### 1.2 v0.5 核心改进

v0.5 引入了 11 项策略来修复 v0.4 的关键缺陷：

| 策略 | 目标 | 状态 |
|------|------|------|
| 时间追踪 | 修复 C-04 (超时配置无效) | ✅ 部分修复 |
| Mock 检测 | 修复 C-02 (Mock 实现检测缺失) | ✅ 部分修复 |
| 权限控制 | 修复 C-03 (权限边界模糊) | ✅ 部分修复 |
| 时间强制限制 | 修复 C-04 (超时配置无效) | ✅ 已修复 |
| 同步检查 | 修复 C-05 (状态同步脆弱) | ✅ 部分修复 |
| 用户介入 | 修复 H-01 (依赖追问被动) | ✅ 已修复 |
| Slaver 分支命名 | 修复 H-03 (Git 历史污染) | ✅ 已修复 |
| Memory Review Agent | 修复 H-05 (记忆无清理) | ✅ 部分修复 |
| 日志轮转 | 修复 H-06 (日志无轮转) | ✅ 已修复 |
| 任务完成验收 | 修复 H-02 (验收无检查) | ✅ 部分修复 |
| Checkpoint/Sprint/Retrospective | 修复 M-01~M-03 | ✅ 已修复 |

### 1.3 v0.5 引入的新风险

**最严重的新问题**:
1. 策略集成缺口 - 多个新脚本未集成到主流程
2. 配置复杂度激增 - 11 项策略引入 50+ 新配置参数
3. 监控代理自举问题 - Memory Review Agent 自身无健康检查
4. 权限配置维护负担 - allow/question/reject 列表需手动维护
5. Sprint 机制与任务超时冲突 - 7 天 Sprint vs 3 小时任务超时

---

## 二、Critical 风险 (🔴)

### C-01: 策略集成缺口

**风险描述**:
v0.5 引入的 11 项策略中，有 3 项未集成到主流程，导致"配置了但无实际作用"。

**影响**:
- Master 监控循环未自动启动
- Memory Review Agent 未自动启动
- Merge Validator 未集成到 merge 流程

**复现路径**:
```
1. 运行 /eket-start 启动实例
2. 检查 Master 监控进程 → 未运行
3. 检查 Memory Review Agent → 未运行
4. 执行 merge 操作 → 无验证步骤
```

**当前代码问题**:
```bash
# eket-start.sh - 无 Master 监控启动
# scripts/memory-review-agent.sh - 独立脚本，无自动启动
# merge-validator.sh - 需要手动调用
```

**修复建议**:
```bash
# eket-start.sh 添加自动启动
start_master_monitoring() {
    if [ "$AUTO_MODE" = true ]; then
        nohup ./scripts/master-monitor.sh > logs/master-monitor.log 2>&1 &
        echo "✓ Master 监控已启动"
    fi
}

start_memory_review_agent() {
    nohup ./scripts/memory-review-agent.sh > logs/memory-review.log 2>&1 &
    echo "✓ Memory Review Agent 已启动"
}

# merge workflow 集成验证
before_merge() {
    ./scripts/merge-validator.sh validate "$TICKET_ID" || {
        echo "❌ 合并验证失败"
        return 1
    }
}
```

---

### C-02: 配置复杂度激增

**风险描述**:
v0.5 引入 11 项策略，config.yml 从 80 行增至 375 行，50+ 配置参数。

**影响**:
- 配置错误风险增加
- 新成员学习成本高
- 调试困难

**当前配置复杂度**:
```yaml
# config.yml v0.5.0 - 375 行
version: "0.5.0"
tasks:
  time:
    default_estimated_minutes: 120
    timeout_minutes: 180
    no_response_timeout_minutes: 30
    human_timeout_hours: 48
  # ... 20+ 参数
monitoring:
  master:
    check_interval_seconds: 60
  slaver:
    heartbeat_interval_seconds: 30
    timeout_seconds: 300
  # ... 15+ 参数
process:
  sprint:
    duration_days: 7
    review_after_sprints: 2
  # ... 10+ 参数
permissions:
  jira:
    allow: []
    question: []
    reject: []
  # ... 30+ 参数
```

**修复建议**:
```yaml
# 简化配置 - 使用预设模板
version: "0.5.1"

# 预设配置
profile: "standard"  # lightweight | standard | enterprise

# 仅暴露关键配置
tasks:
  default_timeout_minutes: 180
  heartbeat_interval_minutes: 5

monitoring:
  enabled: true

sprint:
  duration_days: 7
```

---

### C-03: Memory Review Agent 自举问题

**风险描述**:
Memory Review Agent 负责监控 memory 健康，但自身无健康检查机制。

**影响**:
- Agent 挂掉后无人知晓
- Memory 问题无法检测
- 配置漂移无法发现

**当前代码问题**:
```bash
# memory-review-agent.sh
while true; do
    monitor_memory
    sleep 3600
done
# ❌ 无心跳
# ❌ 无健康检查
# ❌ 无故障转移
```

**修复建议**:
```bash
# 添加健康检查
HEALTH_FILE=".eket/state/memory-agent-health"

report_health() {
    cat > "$HEALTH_FILE" << EOF
status: healthy
last_check: $(date -Iseconds)
last_action: $LAST_ACTION
memory_checked: $MEMORY_COUNT
EOF
}

# Master 监控检查 Agent 健康
check_agent_health() {
    local last_check=$(grep "last_check:" "$HEALTH_FILE" | cut -d' ' -f2)
    local elapsed=$(($(date +%s) - $(date -d "$last_check" +%s)))
    if [ "$elapsed" -gt 3600 ]; then
        alert "Memory Review Agent 可能已挂掉"
    fi
}
```

---

### C-04: 权限配置维护负担

**风险描述**:
allow/question/reject 列表需手动维护，容易过时或配置错误。

**影响**:
- 配置过慢导致 Slaver 频繁询问
- 配置过快导致安全风险
- 新操作需手动更新配置

**当前配置**:
```yaml
permissions:
  jira:
    allow:
      - "read_ticket"
      - "update_status"
      - "add_comment"
    question:
      - "change_priority"
      - "assign_agent"
    reject:
      - "delete_ticket"
      - "change_type"
  # ... 类似配置 30+ 项
```

**修复建议**:
```yaml
# 基于风险的动态权限
permissions:
  model: "risk_based"
  auto_allow_patterns:
    - "read_*"
    - "list_*"
    - "get_*"
  auto_question_patterns:
    - "update_*"
    - "change_*"
    - "create_*"
  auto_reject_patterns:
    - "delete_*"
    - "drop_*"
    - "purge_*"
```

---

### C-05: Sprint 机制与任务超时冲突

**风险描述**:
Sprint 周期 7 天，但任务超时 3 小时，存在时间尺度冲突。

**影响**:
- 长周期任务被强制重置
- Sprint 目标无法完成
- Slaver 士气受挫

**复现场景**:
```
1. Slaver 领取复杂任务 (预估 8 小时)
2. 配置 timeout_minutes: 180 (3 小时)
3. Slaver 遇到问题，人类 4 小时未响应
4. 任务被强制重置为 ready
5. Sprint 目标缺少关键功能
```

**修复建议**:
```yaml
tasks:
  time:
    default_estimated_minutes: 120
    max_estimated_minutes: 480  # 8 小时上限
    timeout_multiplier: 1.5     # 超时 = 预估 * 1.5
    human_timeout_hours: 24     # 人类响应 24 小时
```

---

### C-06: 心跳机制实现不完整

**风险描述**:
心跳机制已定义但无实际监控和告警实现。

**影响**:
- Slaver 停滞无法检测
- 超时后无自动处理
- 配置了但无作用

**当前代码问题**:
```bash
# task-time-tracker.sh - 仅有更新心跳函数
update_heartbeat() {
    echo "last_heartbeat: $(date -Iseconds)" >> "$STATE_FILE"
}

# ❌ 无监控函数
# ❌ 无告警函数
# ❌ 无自动恢复函数
```

**修复建议**:
```bash
# heartbeat-monitor.sh (新增)
check_heartbeats() {
    for state_file in .eket/state/slaver-*.yml; do
        local last=$(grep "last_heartbeat:" "$state_file" | cut -d' ' -f2)
        local elapsed=$(($(date +%s) - $(date -d "$last" +%s)))
        local timeout=$(grep "timeout_seconds:" "$state_file" | cut -d':' -f2 | tr -d ' ')

        if [ "$elapsed" -gt "$timeout" ]; then
            echo "⚠️ Slaver $(basename $state_file) 心跳超时 ($elapsed > $timeout)"
            alert_slaver_timeout "$state_file"
            reset_task_if_needed "$state_file"
        fi
    done
}

# 每分钟检查
while true; do
    check_heartbeats
    sleep 60
done
```

---

### C-07: Mock 检测覆盖不全

**风险描述**:
mock-detector.sh 仅检测固定模式，无法覆盖所有 Mock 场景。

**影响**:
- 高级 Mock 实现可绕过检测
- 部分假代码仍能通过检查

**当前检测模式**:
```bash
# mock-detector.sh
detect_mock_patterns() {
    grep -r "return \[\]" src/
    grep -r "return {}" src/
    grep -r "// TODO" src/
    grep -r "NotImplementedError" src/
    # ❌ 无法检测以下模式:
    # - return mock_data (外部文件)
    # - return fake_response() (函数调用)
    # - async def search() -> List: ... (空异步函数)
}
```

**修复建议**:
```bash
detect_mock_implementations() {
    # 检测空函数体
    for file in $(find src -name "*.ts" -o -name "*.py"); do
        if is_function_empty "$file"; then
            echo "⚠️ 发现空函数体：$file"
            ALERT_FILES+=("$file")
        fi
    done

    # 检测硬编码返回
    grep -rE "return ['\"]mock|fake|dummy" src/
    grep -rE "return \{.*mock.*\}" src/

    # 检测外部 Mock 文件
    find src -name "*mock*" -o -name "*fake*" -o -name "*stub*"
}
```

---

## 三、High 风险 (🟡)

### H-01: 无响应超时配置过短

**风险描述**:
`no_response_timeout_minutes: 30` 对人类来说过短。

**影响**:
- 人类短暂离开导致任务重置
- Slaver 工作成果丢失
- 重复工作增加

**修复建议**:
```yaml
tasks:
  time:
    no_response_timeout_minutes: 120  # 2 小时更合理
    grace_period_minutes: 30          # 重置前宽限期
```

---

### H-02: 任务重置后无通知

**风险描述**:
任务超时重置后，无通知机制告知 Slaver。

**影响**:
- Slaver 继续无效工作
- 状态不一致
- 信任受损

**修复建议**:
```bash
reset_ticket_status() {
    # ... 重置状态后 ...

    # 发送通知
    notify_slaver "$SLAVER_ID" "任务 $TICKET_ID 已重置"
    create_message "inbox/human_feedback/task-reset-$TICKET_ID.md" << EOF
# 任务重置通知

**任务**: $TICKET_ID
**原因**: 超时 $ELAPSED 分钟无响应
**操作**: 状态已重置为 ready
EOF
}
```

---

### H-03: 合并策略过于严格

**风险描述**:
`all_features_complete` 策略要求所有功能完成才能合并。

**影响**:
- 大功能阻塞小功能
- 合并冲突风险增加
- 无法增量交付

**修复建议**:
```yaml
merge:
  strategy: "feature_flagged"  # 支持部分合并
  allow_partial: true
  min_completion_percent: 80
  require_feature_flags: true
```

---

### H-04: Checkpoint 无实际验证

**风险描述**:
Checkpoint 机制已定义但无实际验证逻辑。

**影响**:
- Checkpoint 可跳过
- 质量门禁失效
- 问题流入下一阶段

**修复建议**:
```bash
create_checkpoint() {
    local checkpoint="$1"

    case "$checkpoint" in
        task_start)
            verify_dependencies_defined
            verify_branch_created
            verify_timer_started
            ;;
        task_dev_complete)
            verify_unit_tests_pass
            verify_lint_pass
            verify_coverage_threshold
            ;;
        task_test_complete)
            verify_e2e_tests_pass
            verify_pr_created
            ;;
        task_complete)
            verify_master_review_pass
            verify_all_features_done
            ;;
    esac
}
```

---

### H-05: Sprint Review 无自动化报告

**风险描述**:
Sprint Review 需要手动创建报告。

**影响**:
- 报告质量不一致
- 容易被跳过
- 信息遗漏

**修复建议**:
```bash
# scripts/generate-sprint-report.sh
generate_sprint_report() {
    local sprint_num="$1"
    cat > "outbox/sprint-reports/sprint-$sprint_num.md" << EOF
# Sprint $sprint_num Review

## 完成的任务
$(list_completed_tasks "$sprint_num")

## 未完成的任务
$(list_incomplete_tasks "$sprint_num")

## 代码质量指标
$(generate_code_metrics "$sprint_num")

## 问题与改进
$(list_issues_and_improvements)
EOF
}
```

---

### H-06: Retrospective 无行动计划追踪

**风险描述**:
Retrospective 产生的改进项无追踪机制。

**影响**:
- 改进项被遗忘
- 同样问题重复出现
- Retrospective 流于形式

**修复建议**:
```yaml
process:
  retrospective:
    create_improvement_tasks: true
    track_improvement_completion: true
    review_previous_improvements: true
```

---

### H-07: 日志轮转配置无实际作用

**风险描述**:
`logging.max_files: 10` 配置但无实际轮转实现。

**影响**:
- 日志无限增长
- 磁盘空间耗尽
- 旧日志难以查找

**修复建议**:
```bash
# scripts/log-rotate.sh (新增)
rotate_logs() {
    for log_dir in logs/*/; do
        # 保留最新 10 个日志
        ls -t "$log_dir"*.log | tail -n +11 | xargs -r rm
        # 压缩 3 天前的日志
        find "$log_dir" -name "*.log" -mtime +3 -exec gzip {} \;
        # 删除 30 天前的压缩日志
        find "$log_dir" -name "*.log.gz" -mtime +30 -delete
    done
}

# 每天执行
0 2 * * * ./scripts/log-rotate.sh
```

---

### H-08: 并发控制无实现

**风险描述**:
`max_concurrent_slavers: 3` 配置但无实际限制。

**影响**:
- 资源可能过载
- 多 Slaver 冲突
- 性能下降

**修复建议**:
```bash
# scripts/concurrency-controller.sh
check_concurrency_limit() {
    local active=$(count_active_slavers)
    local max=$(grep "max_concurrent_slavers:" .eket/config.yml | cut -d':' -f2 | tr -d ' ')

    if [ "$active" -ge "$max" ]; then
        echo "❌ 已达最大并发 Slaver 数 ($active/$max)"
        return 1
    fi
    return 0
}

before_claim_task() {
    check_concurrency_limit || {
        echo "等待其他 Slaver 完成任务..."
        wait_for_slot
    }
}
```

---

### H-09: Worktree 清理策略缺失

**风险描述**:
Worktree 无自动清理机制，可能无限增长。

**影响**:
- 磁盘空间耗尽
- 历史 worktree 混淆
- 管理困难

**修复建议**:
```yaml
git:
  worktree:
    auto_prune: true
    prune_after_days: 7
    keep_last_n: 5
```

---

## 四、Medium 风险 (🟢)

### M-01: 任务优先级算法简单

**风险描述**:
优先级计算仅基于简单加分，未考虑复杂度、依赖等。

**修复建议**:
```yaml
tasks:
  priority:
    algorithm: "weighted_scoring"
    factors:
      business_value: 0.3
      urgency: 0.25
      dependencies: 0.2
      effort: 0.15
      risk: 0.1
```

---

### M-02: 人类反馈解析简单

**风险描述**:
人类反馈仅通过 grep 关键词解析，容易误解。

**修复建议**:
- 使用结构化反馈表单
- 引入 NLP 解析
- 支持多轮对话确认

---

### M-03: 文档生成质量不稳定

**风险描述**:
Confluence 文档质量依赖 AI 水平，无一致性保证。

**修复建议**:
- 引入文档模板
- 添加文档审查环节
- 建立文档质量标准

---

### M-04: 测试覆盖率阈值过低

**风险描述**:
默认覆盖率要求 80%，对核心功能不足。

**修复建议**:
```yaml
testing:
  coverage:
    default_min: 80
    critical_modules_min: 95
    require_branch_coverage: true
```

---

### M-05: API 文档同步滞后

**风险描述**:
API 文档不随代码自动更新。

**修复建议**:
- 使用 OpenAPI/Swagger
- 代码即文档
- 变更时自动提醒更新

---

### M-06: 错误处理不一致

**风险描述**:
各脚本错误处理方式不一致。

**修复建议**:
- 统一错误码规范
- 统一日志格式
- 统一退出码

---

### M-07: 配置验证缺失

**风险描述**:
config.yml 无验证机制，错误配置可导致系统异常。

**修复建议**:
```bash
# scripts/validate-config.sh
validate_config() {
    # 检查必填字段
    # 检查值范围
    # 检查依赖配置
    echo "配置验证通过"
}
```

---

### M-08: 无性能基准测试

**风险描述**:
框架性能无基准测试，无法评估优化效果。

**修复建议**:
- 建立性能基准
- 定期运行性能测试
- 追踪性能趋势

---

## 五、Low 风险 (⚪)

### L-01: 命令别名缺失

无便捷命令别名，如 `/es` 代替 `/eket-status`。

---

### L-02: 进度可视化简单

仅支持文本状态，无图形化 Dashboard。

---

### L-03: 通知渠道单一

仅支持文件通知，无 Slack/邮件/钉钉集成。

---

### L-04: 快捷键缺失

无交互式快捷键支持。

---

### L-05: 配置热重载不支持

修改配置需重启实例。

---

## 六、修复优先级矩阵

| 风险 ID | 风险等级 | 影响范围 | 修复难度 | 优先级 | 建议 Sprint |
|--------|---------|---------|---------|--------|------------|
| C-01 | Critical | 高 | 低 | P0 | Sprint 1 |
| C-02 | Critical | 中 | 中 | P0 | Sprint 1 |
| C-03 | Critical | 中 | 低 | P0 | Sprint 1 |
| C-04 | Critical | 高 | 低 | P0 | Sprint 1 |
| C-05 | Critical | 中 | 低 | P0 | Sprint 1 |
| C-06 | Critical | 高 | 中 | P0 | Sprint 1 |
| C-07 | Critical | 高 | 中 | P0 | Sprint 1 |
| H-01 | High | 中 | 低 | P1 | Sprint 2 |
| H-02 | High | 中 | 低 | P1 | Sprint 2 |
| H-03 | High | 中 | 中 | P1 | Sprint 2 |
| H-04 | High | 高 | 中 | P1 | Sprint 2 |
| H-05 | High | 低 | 低 | P1 | Sprint 2 |
| H-06 | High | 低 | 中 | P1 | Sprint 2 |
| H-07 | High | 中 | 低 | P1 | Sprint 2 |
| H-08 | High | 中 | 中 | P1 | Sprint 2 |
| H-09 | High | 低 | 低 | P1 | Sprint 2 |

---

## 七、与 v0.4 风险对比

### 7.1 已修复的 v0.4 风险

| v0.4 风险 | v0.5 状态 | 修复程度 |
|----------|----------|---------|
| C-01 Slaver 追踪缺失 | ✅ 部分修复 | 心跳机制已实现，监控待集成 |
| C-02 Mock 检测缺失 | ✅ 部分修复 | 基础检测已实现，高级模式待补充 |
| C-03 权限边界模糊 | ✅ 部分修复 | 配置已定义，自动维护待实现 |
| C-04 任务超时配置无效 | ✅ 已修复 | 时间追踪脚本已实现 |
| C-05 状态同步脆弱 | ✅ 部分修复 | 同步检查已定义，错误处理待完善 |

### 7.2 v0.5 新增风险

| 新增风险 | 风险等级 | 说明 |
|---------|---------|------|
| 策略集成缺口 | Critical | 3 项策略未集成到主流程 |
| 配置复杂度激增 | Critical | 50+ 配置参数增加维护成本 |
| Agent 自举问题 | Critical | Memory Review Agent 无健康检查 |
| Sprint/超时冲突 | Critical | 7 天 Sprint vs 3 小时任务超时 |
| 权限配置负担 | Critical | allow/question/reject 需手动维护 |

---

## 八、总结与建议

### 8.1 风险分布对比

```
v0.4:                    v0.5:
Critical: ████████ 5     Critical: ████████████ 7 (+2)
High:     ████████████ 8 High:     ████████████████ 9 (+1)
Medium:   ██████████ 6   Medium:   ██████████████ 8 (+2)
Low:      ██████ 4       Low:      ████████ 5 (+1)
```

### 8.2 核心问题

**v0.5 的主要问题是"集成债务"**：
- 11 项策略独立开发，但未完全集成到主流程
- 配置复杂度激增，但无简化机制
- 新增 Agent 无健康检查，形成"监控盲区"

### 8.3 修复建议优先级

**立即修复 (Sprint 1)**:
1. 集成 Master 监控循环到 /eket-start
2. 集成 Memory Review Agent 自启动
3. 集成 Merge Validator 到 merge 流程
4. 实现心跳监控和告警
5. 简化配置 - 引入 preset 模板
6. 完善 Mock 检测模式

**本周修复 (Sprint 2)**:
1. 调整无响应超时为 2 小时
2. 添加任务重置通知
3. 实现 Checkpoint 验证逻辑
4. 实现日志轮转脚本
5. 实现并发控制

**规划修复 (Sprint 3+)**:
1. 基于风险的动态权限
2. Sprint/超时冲突解决
3. 自动化 Sprint 报告
4. Retrospective 追踪

### 8.4 长期建议

1. **建立集成测试** - 确保策略集成到主流程
2. **配置即代码** - 使用 schema 验证配置
3. **健康检查体系** - 所有 Agent 都有健康报告
4. **文档生成自动化** - 减少人工维护成本
5. **可观测性建设** - 日志、指标、追踪三位一体

---

**审查人**: Master Node + AI Agent
**审查日期**: 2026-03-23
**下次审查**: Sprint 1 完成后复查 Critical 风险
