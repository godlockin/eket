# TASK-Z07: SWE-bench Agent 能力增强

**EPIC**: EPIC-014  
**状态**: done  
**优先级**: P0  
**预估**: 5d  
**负责人**: 待分配

---

## 背景

当前 SWE-bench 模拟通过率 36%，低于行业平均水平。

| Agent | 得分 | 差距 |
|-------|------|------|
| OpenHands | 77.6% | +41.6% |
| Claude 3.5 | 49% | +13% |
| **EKET** | **36%** | 基准 |
| GPT-4o | 33.2% | -2.8% |

## 目标

将 SWE-bench 模拟通过率从 36% 提升到 45%+。

## 改进方向

### 1. 代码上下文检索 (RAG)

当前问题：Agent 无法获取相关代码上下文

改进：
```python
def get_relevant_context(repo, issue):
    # 1. 解析 issue 关键词
    # 2. 搜索相关文件
    # 3. 提取函数/类定义
    # 4. 构建上下文 prompt
```

### 2. Patch 生成优化

当前问题：生成的 patch 格式错误率高

改进：
- 使用 Few-shot examples
- 添加格式验证
- 迭代修复

### 3. 自动验证反馈

当前问题：无法验证 patch 正确性

改进：
```python
def validate_and_retry(patch, tests):
    # 1. 应用 patch
    # 2. 运行测试
    # 3. 分析失败原因
    # 4. 生成改进版 patch
    # 5. 最多重试 3 次
```

## 任务清单

### Phase 1: 上下文检索 (2d)
- [ ] 实现代码搜索
- [ ] 实现相关文件提取
- [ ] 集成到 agent_executor.py

### Phase 2: Patch 优化 (2d)
- [ ] 添加 few-shot examples
- [ ] 实现格式验证器
- [ ] 添加重试逻辑

### Phase 3: 验证集成 (1d)
- [ ] 实现测试运行器
- [ ] 实现反馈循环
- [ ] 运行评测验证

## 验收标准

- [ ] SWE-bench 模拟 300 题通过率 >= 45%
- [ ] Patch 格式错误率 < 10%
- [ ] 平均重试次数 < 2

## 技术方案

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│   Issue     │───▶│  RAG 检索    │───▶│  上下文     │
└─────────────┘    └──────────────┘    └─────────────┘
                                              │
                                              ▼
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│   验证      │◀───│  Patch 生成  │◀───│  Prompt     │
└─────────────┘    └──────────────┘    └─────────────┘
       │
       ▼ 失败
┌─────────────┐
│  反馈重试   │
└─────────────┘
```

---

## 变更日志

| 日期 | 变更 | 操作人 |
|------|------|--------|
| 2026-05-25 | 创建 Ticket | Master |
