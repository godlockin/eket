# TASK-E12-004: 添加 Cursor IDE 支持

## 元信息

| 字段 | 值 |
|------|-----|
| Epic | EPIC-012 |
| 优先级 | P2 |
| 估时 | 1.5h |
| 状态 | `done` |
| 依赖 | 无 |

## 背景

andrej-karpathy-skills 同时支持 Claude Code 和 Cursor IDE，通过 `.cursor/` 目录实现跨 IDE 兼容。

## 目标

为 EKET 添加 Cursor IDE 支持，让用户在 Cursor ts/           # 可选的提示词模板
```

### 2. rules 文件内容

```markdown
# EKET Framework Rules for Cursor

## Identity
Read `.eket/IDENTITY.md` to confirm role (Master or Slaver).

## Workflow
- Master: Coordinate, review, merge
- Slaver: Implement, test, submit PR

## Key Commands
- `eket task:claim` — Claim a task
- `eket gate:review` — Pre-commit review
- `eket task:complete` — Complete task

## Behavior Guidelines
1. **Think Before Coding** — State assumptions explicitly
2. **Simplicity First** — Minimum code solving the problem
3. **Surgical Changes** — Edit only what's required
4. **Goal-Driven** — Define verifiable success criteria

## Branch Strategy
All changes on `feature/*` branches. Never commit to main directly.
```

### 3. CURSOR.md 同步

创建 `CURSOR.md` 作为 Cursor 专用入口，内容精简版 CLAUDE.md。

## 验收标准

- [ ] 创建 `.cursor/rules` 文件
- [ ] 创建 `CURSOR.md` 入口文件
- [ ] Cursor 能正确识别并应用规则
- [ ] 与 CLAUDE.md 核心内容一致

## 测试

1. 在 Cursor 中打开 EKET 项目
2. 验证 AI 助手能识别 EKET 角色
3. 验证行为指南被应用
